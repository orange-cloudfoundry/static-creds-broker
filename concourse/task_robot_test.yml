---
platform: linux
image: docker:///tao1101/robotframework_cf-cli

inputs:
  - name: static-creds-broker
  - name: release

outputs:
  - name: report

run:
  path: robot 
  args: ["--pythonpath", "static-creds-broker/acceptance/classes/", "--outputdir", "./report/", "static-creds-broker/acceptance/"]