*** Settings ***
Resource        ../keywords.robot

*** Variables ***
${USE_YAML_CONFIG} 		${true}
${BROKER_APP_NAME} 		${BROKER_APP_NAME_LOCAL_CONFIG}
${BROKER_HOSTNAME} 		${BROKER_HOSTNAME_LOCAL_CONFIG}
${BROKER_NAME} 			${BROKER_NAME_LOCAL_CONFIG}